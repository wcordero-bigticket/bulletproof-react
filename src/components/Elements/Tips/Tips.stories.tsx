import { Meta } from '@storybook/react';

import { Tips } from './Tips';

const meta: Meta = {
  title: 'Components/Elements/Tips',
  component: Tips,
  parameters: {
    controls: { expanded: true },
  },
};

export default meta;
